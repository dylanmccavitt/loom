# Optional user-level Codex profile template.
# Candidate destination after approval: ~/.codex/omp-harness.config.toml
# Select explicitly with: codex --profile omp-harness

model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[features]
multi_agent = true
