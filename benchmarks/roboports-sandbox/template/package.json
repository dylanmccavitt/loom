{
  "name": "roboports-sandbox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tiny inventory-tracker library used as an agent benchmark target.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
